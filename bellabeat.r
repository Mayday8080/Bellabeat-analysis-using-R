{"metadata":{"kernelspec":{"name":"ir","display_name":"R","language":"R"},"language_info":{"name":"R","codemirror_mode":"r","pygments_lexer":"r","mimetype":"text/x-r-source","file_extension":".r","version":"4.0.5"}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"## **Problem Statement**\nThe main goals of this analysis is to:\n\n1. Identify trends in smart device usage \n2. Apply these trends to Bellabeat customers\n3. Record the effects of these trends on Bellabeat's marketing strategy\n\n## \"**Data**\"\n\nThe dataset was obtain from Kaggle, licensed under CCO: Public Domaib and was made available through [Mobius](https://www.kaggle.com/arashnic). The dataset contains personal fitness tracker from thirty fitbit users who consented to the submission of personal tracker data which includes:\n\n* minute-level output for physical activity\n* heart rate\n* sleep monitoring\n* steps\n\n## **Processing the data**\n\nFirst, the dataset was downloaded from Kaggle and then uploaded to RStudio cloud. \n\n","metadata":{}},{"cell_type":"code","source":"library(tidyverse)","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:50:41.221499Z","iopub.execute_input":"2023-03-27T06:50:41.223204Z","iopub.status.idle":"2023-03-27T06:50:41.237852Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"daily_activity <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/dailyActivity_merged.csv\")\nhead(daily_activity)\ncolnames(daily_activity)\n\ndaily_calories <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/dailyCalories_merged.csv\")\nhead(daily_calories)\ncolnames(daily_calories)\n\ndaily_intensities <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/dailyIntensities_merged.csv\")\nhead(daily_intensities)\ncolnames(daily_intensities)\n\ndaily_steps <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/dailySteps_merged.csv\")\nhead(daily_steps)\ncolnames(daily_steps)\n\ndaily_sleep <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/sleepDay_merged.csv\")\nhead(daily_sleep)\ncolnames(daily_sleep)\n\nweight_info <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/weightLogInfo_merged.csv\")\nhead(weight_info)\ncolnames(weight_info)\n\nheartrate_secs <- read.csv(\"../input/fitbit/Fitabase Data 4.12.16-5.12.16/heartrate_seconds_merged.csv\")\nhead(heartrate_secs)\ncolnames(heartrate_secs)","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:50:41.247061Z","iopub.execute_input":"2023-03-27T06:50:41.248721Z","iopub.status.idle":"2023-03-27T06:50:46.902191Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"## **Cleaning Data**\nChanged the date and time formats to be consistent\n","metadata":{}},{"cell_type":"code","source":"daily_activity$ActivityDate <- as.Date(daily_activity$ActivityDate, \"%m/%d/%Y\")\ndaily_sleep$ActivityDate <- parse_date_time(daily_sleep$SleepDay, orders = \"mdy HMS\")\ndaily_sleep$ActivityDate <- as.Date(daily_sleep$ActivityDate, \"%m/%d/%y %h:%m:%s\")","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:50:46.904741Z","iopub.execute_input":"2023-03-27T06:50:46.906108Z","iopub.status.idle":"2023-03-27T06:50:46.949446Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"Checking and removing duplicates for duplicates","metadata":{}},{"cell_type":"code","source":"sum(duplicated(daily_activity))\n\nsum(duplicated(daily_sleep))\ndaily_sleep <- distinct(daily_sleep)\nsum(duplicated(daily_sleep))\n\nsum(duplicated(daily_calories))\n\nsum(duplicated(daily_intensities))\n\nsum(duplicated(daily_steps))\n\nsum(duplicated(weight_info))\n\nsum(duplicated(heartrate_secs))","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:04.093014Z","iopub.execute_input":"2023-03-27T06:51:04.095662Z","iopub.status.idle":"2023-03-27T06:51:12.602992Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"Checking for empty values","metadata":{}},{"cell_type":"code","source":"is.null(daily_activity)\nis.null(daily_calories)\nis.null(daily_intensities)\nis.null(daily_sleep)\nis.null(daily_steps)\nis.null(weight_info)\nis.null(heartrate_secs)","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:22.543781Z","iopub.execute_input":"2023-03-27T06:51:22.545753Z","iopub.status.idle":"2023-03-27T06:51:22.598029Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"## **Analyzing data**","metadata":{}},{"cell_type":"code","source":"n_distinct(daily_activity$Id)\nsummary(daily_activity)\nn_distinct(daily_calories$Id)\nn_distinct(daily_intensities$Id)\nn_distinct(daily_sleep$Id)\nn_distinct(daily_steps$Id)\nn_distinct(weight_info$Id)\nn_distinct(heartrate_secs$Id)","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:26.429661Z","iopub.execute_input":"2023-03-27T06:51:26.431422Z","iopub.status.idle":"2023-03-27T06:51:26.581047Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"Clearly, most fitbit users use smart devices frequently to track their daily activity, calories, intensities and steps and just a few use it to check their weight.","metadata":{}},{"cell_type":"code","source":"ggplot(data = daily_activity)+ geom_smooth(mapping = aes(x = TotalDistance,y=Calories))","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:34.104388Z","iopub.execute_input":"2023-03-27T06:51:34.106864Z","iopub.status.idle":"2023-03-27T06:51:35.950141Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"The graph above shows a positive correlation between the distance taken and the calories burned.","metadata":{}},{"cell_type":"code","source":"daily_activity %>% \n  mutate(weekdays = weekdays(as.Date(ActivityDate))) %>% \n  select(weekdays, TotalSteps) %>% \n  mutate(weekdays = factor(weekdays, levels = c('Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'))) %>% \n  drop_na() %>%\n  ggplot(aes(x= weekdays, y= TotalSteps/1000)) +\n  geom_col(fill=\"purple\",alpha = 0.6)+\n  labs(title = \"Total steps taken\",subtitle=\"Total number of steps taken per weekday\", y= \"Total steps\")","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:52.405060Z","iopub.execute_input":"2023-03-27T06:51:52.406860Z","iopub.status.idle":"2023-03-27T06:51:53.143600Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"The graph above depicts that the highest number of steps are usually taken on Sunday followed by Monday and the least number of steps are taken on Saturday.\n\nAccording to the sleep physicians and scientists of National Center for Chronic Disease Prevention and Health Promotion([NCCDPHP](https://www.cdc.gov/sleep/about_sleep/how_much_sleep.html)) and [Sleep Foundation](https://www.sleepfoundation.org/how-sleep-works/how-much-sleep-do-we-really-need) studies, it is highly recommended for adults to have at least 7~9 hours or on average 8 hours of sleeping time. So let's apply it to our results.","metadata":{}},{"cell_type":"code","source":"sleep_dur <- round(daily_sleep$TotalMinutesAsleep/60)\navgsleep_dur <- round(mean(sleep_dur))\navgsleep_dur","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:51:57.022357Z","iopub.execute_input":"2023-03-27T06:51:57.024033Z","iopub.status.idle":"2023-03-27T06:51:57.048443Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"The average sleep of Fitbit users was 7 hours a day. The histogram below shows the distribution of sleep.","metadata":{}},{"cell_type":"code","source":"ggplot(data=daily_sleep) + geom_histogram(mapping=aes(x=sleep_dur), binwidth = 1, color=\"purple\", fill=\"purple\", alpha = 0.6)+\n\n  geom_vline(mapping=aes(xintercept=mean(sleep_dur, na.rm=T)), color=\"cyan\", \n             linetype=\"dashed\") + labs(title = \"Distribution of sleep duration\", x= \"sleep_duration\")","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:52:01.044599Z","iopub.execute_input":"2023-03-27T06:52:01.047424Z","iopub.status.idle":"2023-03-27T06:52:01.391561Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"daily_sleep %>% \n  mutate(weekdays = weekdays(as.Date(SleepDay))) %>% \n  select(weekdays, TotalMinutesAsleep) %>% \n  mutate(weekdays = factor(weekdays, levels = c('Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'))) %>% \n  drop_na() %>%\n  ggplot(aes(x= weekdays, y= round((TotalMinutesAsleep)/60))) +\n  geom_col(fill=\"purple\",alpha = 0.6)+\n  labs(title =\"Duration of sleep\", subtitle = \"The total duration of sleep per weekday\", y=\"hours slept\")","metadata":{"execution":{"iopub.status.busy":"2023-03-27T06:52:19.403078Z","iopub.execute_input":"2023-03-27T06:52:19.405050Z","iopub.status.idle":"2023-03-27T06:52:19.701699Z"},"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"As we noticed earlier, Sunday is when most steps were take and apparently it is also the day where users sleep for longer periods. However there is not enough evidence to conclude that the number of steps is a causation.\n\n## **Recommendations**\n1.Bellabeat can do advertisements on Sunday since it is the most active day for users.\n\n2.Give a free trail version of the Bellabeat membership, which can customize and auto generate the exercises as per the user benefits with maybe certain \"awards\" like a chance to win Bellabeat products(Classic Wellness Tracker, Wellness clock, Water Bottle) if they remain loyal to their routine.\n\n3. Finally, collect data from more users and in longer time period for better results.","metadata":{}}]}